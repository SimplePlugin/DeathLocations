name: DeathLocations
version: '${version}'
main: me.truedarklord.deathLocations.DeathLocations
api-version: '1.20'
prefix: DeathLocations
authors: [ TrueDarkLord ]
description: Does things on player's death.
commands:
  reload:
    description: Reloads the plugin.
    permission: deathchests.reload
    usage: /reload

folia-supported: true